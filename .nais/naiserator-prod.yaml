apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: skattetrekk-backend
  namespace: okonomi
  labels:
    team: okonomi
spec:
  image: "{{{ image }}}"
  port: 8080
  ingresses:
    - https://skattetrekk-backend.intern.nav.no
    - https://skattetrekk-backend.prod-fss-pub.nais.io
  liveness:
    path: actuator/health/liveness
    initialDelay: 30
  readiness:
    path: actuator/health/readiness
    initialDelay: 30
  startup:
    path: actuator/health/readiness
    initialDelay: 30
    failureThreshold: 30
    periodSeconds: 5
  tokenx:
    enabled: true
  azure:
    application:
      enabled: true
  replicas:
    min: 2
    max: 2
  resources:
    limits:
      memory: 4096Mi
    requests:
      cpu: 100m
      memory: 512Mi
  prometheus:
    enabled: true
    path: actuator/prometheus
  observability:
    autoInstrumentation:
      enabled: true
      runtime: java
  webproxy: true
  accessPolicy:
    inbound:
      rules:
        - application: skattetrekk-frontend
          namespace: okonomi
          cluster: prod-gcp
    outbound:
      rules:
        - application: sokos-oppdrag-proxy
          namespace: okonomi
        - application: logging
          namespace: nais-system

  env:
    - name: SPRING_PROFILES_ACTIVE
      value: default
    - name: TREKK_URL
      value: http://oppdrag-rest-proxy.okonomi.svc.nais.local
    - name: TREKK_SCOPE
      value: api://prod-fss.okonomi.oppdrag-rest-proxy/.default
    - name: TREKK_AUDIENCE
      value: prod-fss:okonomi:oppdrag-rest-proxy
